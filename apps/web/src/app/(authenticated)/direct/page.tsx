import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Direct",
};

export default function DirectMessagesPage() {
  return (
    <div>
      <p>Direct messages</p>
    </div>
  );
}
